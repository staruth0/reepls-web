/**
 * Shared content width and padding for all landing sections and header.
 * Use this so section contents align vertically on one axis from nav to footer.
 */
export const SECTION_CONTENT_CLASS =
  "max-w-7xl mx-auto px-4 sm:px-6 md:px-8 lg:px-12 xl:px-16";

/** Consistent vertical padding for all sections (small and large screens). */
export const SECTION_PADDING_Y =
  "py-12 sm:py-16 md:py-20 lg:py-24";
